x = 0 #set inital values for x and y
y = 1

term = 1 #term variable to track what term of the sequence its on

while (len(str(y)) != 1000): #convert y to a string and count the number of 'letters' (digits) of the current term break if its 1000 digits long
    t = y                 #set the value of y to a temp var
    y = y + x             #set y to y plus the previous number in the sequence (x)
    x = t                 #set x to the temp variable
    term = term + 1       #Increment the term varialbe for each loop

print(term)
print (y)
