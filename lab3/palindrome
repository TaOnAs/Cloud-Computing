word = input() #Gets user input and stores it in a variable word
reverseWord = word[::-1] #reverse the string input

print(word)         #prints out the entered word
print (reverseWord) # prints out the reversed word

if word == reverseWord: #if the inputted word is equal to the reversed word print true else print false
  print(True)
else:
  print(False)
